# Manual QA Matrix

Date: 2026-04-24

| ID сценария | Сценарий | Предусловия | Шаги | Ожидаемый результат | Фактический результат | Статус | Артефакт доказательства |
| --- | --- | --- | --- | --- | --- | --- | --- |
| QA-01 | Запуск frontend + backend | Локальные зависимости подняты, backend доступен по `http://localhost:8181` | 1. Запустить backend. 2. Запустить frontend. 3. Открыть UI. | UI открывается без фатальной ошибки, базовые запросы не падают на старте. | Не выполнено в этой сессии как полный ручной прогон. | Pending manual run | Скриншот стартового экрана, backend startup log |
| QA-02 | Создание новой беседы | UI открыт, backend отвечает | 1. Создать новую беседу. 2. Проверить список бесед. | Новая беседа появляется в интерфейсе и доступна для выбора. | Не выполнено в этой сессии. | Pending manual run | Скриншот списка бесед, network trace |
| QA-03 | Отправка обычного сообщения в simple mode | Новая или существующая беседа | 1. Переключиться в simple mode. 2. Отправить сообщение. | Появляется непустой ответ ассистента без SSE-ошибки. | Не выполнено в этой сессии. | Pending manual run | Скриншот ответа, backend request log |
| QA-04 | Отправка сообщения в agent mode через SSE | Беседа доступна, backend работает | 1. Переключиться в agent mode. 2. Отправить сообщение. | Появляется первый SSE event или контролируемое сообщение об ошибке, UI не зависает. | Частично подтверждено backend runtime evidence на 2026-04-23: SSE events приходили, но полный UI ручной прогон не выполнялся. | Partial backend evidence only | SSE console/network trace, backend runtime log |
| QA-05 | Генерация заголовка беседы | Беседа существует | 1. Вызвать генерацию заголовка после первого сообщения. | Заголовок генерируется и сохраняется либо показывается контролируемая ошибка. | В live backend на 2026-04-23 был зафиксирован DB/R2DBC blocker, поэтому сценарий не подтвержден как успешный. | Blocked by runtime dependency | Backend error response, DB/R2DBC blocker note |
| QA-06 | Обработка ошибки backend в UI | Backend доступен, можно спровоцировать ошибку | 1. Отправить запрос при заведомо проблемном backend состоянии. | UI показывает контролируемую ошибку без падения приложения. | Не выполнено в UI; backend controlled error path подтвержден focused tests. | Partial backend evidence only | Focused test output, future UI screenshot |
| QA-07 | Недоступность model provider | Backend поднят с недоступным провайдером или test key | 1. Отправить agent-mode сообщение. | Сценарий завершается контролируемой ошибкой, без бесконечного ожидания. | Частично подтверждено backend runtime evidence на 2026-04-23. | Partial backend evidence only | Backend runtime log, SSE error event |
| QA-08 | Недоступность внешнего сервиса / DB | Backend поднят с проблемной БД или интеграцией | 1. Вызвать список бесед или title generation. | Возвращается контролируемая ошибка, факт сбоя фиксируется в логах. | Подтверждено на 2026-04-23: `/api/conversations` и title generation упирались в backend storage/connectivity errors. | Confirmed blocker evidence | Error response sample, backend log excerpt |
| QA-09 | Восстановление после ошибки | Предыдущий failure scenario уже произошел | 1. Восстановить зависимость. 2. Повторить запрос. | Повторный запрос обрабатывается штатно. | Не выполнено в этой сессии. | Pending manual run | Сравнительные скриншоты до/после |
